# API
NODE_ENV=development
PORT=3000

# PostgreSQL
# Inside Docker Compose, use the db service hostname.
DATABASE_URL=postgres://user_admin:change-me@db:5432/groadmin_db

# Auth
# Use a long random value in every real environment.
JWT_SECRET=replace-with-a-long-random-secret
